apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: desbroces-pontevedra-tls
  namespace: default
spec:
  secretName: desbroces-pontevedra-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - desbroces-pontevedra.fun

